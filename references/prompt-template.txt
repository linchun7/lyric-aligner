请按当前 Skill 的“音乐混剪精校字幕工作流”处理本次任务，并直接开始执行，不要先只给方案。

本次输入：
- 任务名：<简短任务名>
- 混剪音频：<绝对路径>
- 剪映字幕 SRT：<绝对路径>
- 歌曲清单：<绝对路径>
- 每首歌语言标注：<例如 zh / en / ko / ja / mixed；可另附 TXT，或逐首写在此处>
- BPM 变化清单：<绝对路径；没有就写“无”>
- 原曲音频目录：<绝对路径>
- 规范 LRC 目录：<绝对路径>
- 以前人工精校过的同一版文件或确认记录：<绝对路径；没有就写“无”>
- 其他说明：<已知旁白、歌曲剪切、版本差异、帧率等；没有就写“无”>

处理要求：
1. 默认使用本目录 `scripts/redo_karaoke_pipeline.py` 的算法 v3.7，并遵守 `references/workflow.md`；v3.6 只作回滚，不另起一套临时逻辑。
2. 剪映字幕格是主要时间参考，但不是绝对正确；剪映文字可能完全错误。不能只按 BPM 推算时间。
3. 联合使用剪映格、规范 LRC、原曲波形、BPM 比例、多语言 ASR、发音相似度和歌词前后顺序完成对齐。
4. 韩文、日文等被剪映识别成英文谐音或留空时必须补齐，不能因没有剪映格而漏词。韩文可使用近似罗马音与专用 ASR；日文含汉字时必须增加假名/汉字读音证据，若当前工具尚未启用该层，则把少量歧义时间点发到对话中确认，不得虚报自动确认。
5. 歌曲中间可能剪掉一段。必须检测原曲时间跳跃、使用分段映射，并明确登记被剪掉的 LRC；不能把被剪歌词补回混剪。
6. 正确歌词允许跨剪映格切分。优先保持实际唱词在剪映格中的字词边界；剪映边界经波形或词级证据证明错误时，才成对修改前格结束和后格开始。
7. 检查：漏句、前后格挪词、整句覆盖相邻句、错误去重、真实重复副歌、跨格连字符、LRC 顺序倒退、未登记剪切和新增重叠。英文按词、中文按字、韩文按音节/短词、日文按假名或读音单位重点检查相邻格首尾 1–3 个单位；剪映有明确边缘词时，不能仅按 LRC 换行移动。
8. Uh/Oh/Yeah/Ah/Ooh/Huh/Na-na 等统称短促衬词（ad-lib vocalizations／无实义发声）。只有规范 LRC 中存在的衬词才保留；LRC 未收录时，即使剪映、ASR、原曲和混剪都检测到，也直接不保留、不列为可选项。默认不启用完整发声模式。
9. 字幕短、语速快或字符/秒高不算错误；只判断歌词和时间是否对应音频。
10. 对自动候选先由大模型结合上下文和所有证据确认。仍无法可靠确认的项目，直接把“时间点 + 当前文本 + 候选文本 + 需要听什么”发在对话中，不发表格。
11. 已确认问题写入本任务的回归数据；重跑一首歌不得改变其他歌曲。不要把旧混剪的 cue 编号或时间覆盖直接套到新混剪，除非输入文件哈希一致。
12. 草稿和审计文件放在任务目录中；只交付一个名称明确的 <任务名>_FINAL.srt。
13. 最终 QA 至少满足：publish_ready=true、issues=0、review_candidate_count=0、unexpected_overlap_count=0、lyric_index_regression_count=0、unresolved_lyric_gap_count=0、unreviewed_audio_edit_candidate_count=0、noncanonical_vocalization_count=0。
14. 完成后在对话中说明终稿路径、QA 路径、是否仍有需人工听的时间点、使用了多少旧人工证据和多少本次新增确认。不要把“QA 通过”表述成未经测量的绝对正确率。

直接开始处理；除非缺少无法替代的输入，不要停在方案阶段。
