# 2026-09-08 本地维护交付快照

交付标记：`maintenance-20260908`。产品身份仍为`4.0.0a19`；这是源码维护快照，不是新的生产准确率封板版本，也不移动原有prod tag。提交前基线为`311bc7f12f1cfc50c87f988137fcd89a60ba5738`。

## 保留项

- 现有Standard / Smart / Pro / Max分层与已有生产校准规则。
- 已实现的人工确认复用、试听工具、剪映区域恢复、字符归属兼容、配置错误检查以及相关执行/回归修复。
- source shadow、HFA/joint上下文、FLOAT解码与逐段语言检测等历史实验代码及测试，保持显式选择，不提升为生产默认。
- 正负实验登记及维护交接，后续按[维护收敛约定](maintenance-convergence-2026-09-08.md)执行。

未证明通用精度收益的选项不默认推广；不再因候选增多而继续叠加算法。历史实现和证据保留，方便复现与定位回归。

## 验证

提交源码与维护测试快照一致：隔离Python3.12共1642项，1638执行通过，4项轻量环境缺PyAV跳过；完整音频环境另跑4/4通过。WALK重新生成和同区域重复恢复均为876条，与上一版SRT字节一致。真实字幕精度没有新增声明。完整Python3.10/3.14及远程CI矩阵未在本地快照流程执行。

本地详细证据位于`output/maintenance_convergence_20260908/`；本次范围、归档校验、独立复核和最终提交收据位于`output/local_delivery_20260908/`。这些记录及媒体按本地保留规则保存，不纳入源码提交。

## 取回与回滚

读取本快照用`git show maintenance-20260908`。需要复现时，在一个尚不存在的新目录创建独立检出：

```powershell
git worktree add --detach <新目录> maintenance-20260908
```

需要研究本次快照之前的代码，可将上面的ref改为`311bc7f12f1cfc50c87f988137fcd89a60ba5738`。使用独立目录保留当前未提交内容；不建议用硬重置覆盖工作目录。旧提交是否适合某份生产输入仍须按其原有产物身份核验。

源码ZIP与SHA256随本地收据保存。它包含代码、测试、参考文档和随附许可，不包含private/output、模型、音视频、人工标注或临时比较脚本；不是带模型与媒体的离线运行包。回滚源码不会自动回滚已生成字幕，也不会删除本地人工确认和历史产物。

交付范围为本地commit、tag与源码归档；远程推送和公开发布另行处理。
